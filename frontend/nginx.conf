server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # single-page app: send unknown paths to index.html so React Router works
    location / {
        try_files $uri /index.html;
    }
}
